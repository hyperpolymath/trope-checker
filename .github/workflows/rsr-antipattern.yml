# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
# RSR Anti-Pattern Check - Uses reusable workflow from standards

name: "🔴 Gate: RSR anti-pattern scan"
on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

jobs:
  antipattern-check:
    uses: hyperpolymath/standards/.github/workflows/rsr-antipattern-reusable.yml@fcb8669169b4e9f5d9848608df880ae5fae812b4 # standards main pin (freezes current behaviour)
