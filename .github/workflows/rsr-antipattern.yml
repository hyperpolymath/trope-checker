# SPDX-License-Identifier: MPL-2.0
# RSR Anti-Pattern Check - Uses reusable workflow from standards

name: "🔴 Gate: RSR anti-pattern scan"
on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

jobs:
  antipattern-check:
    uses: hyperpolymath/standards/.github/workflows/rsr-antipattern-reusable.yml@8fbf5cd88b43a699ef09fca2f83a852281e269dd # standards main pin (freezes current behaviour)
