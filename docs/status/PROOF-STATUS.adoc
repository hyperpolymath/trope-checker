// SPDX-FileCopyrightText: © 2026 Jonathan D.A. Jewell (hyperpolymath) <j.d.a.jewell@open.ac.uk>
// SPDX-License-Identifier: MPL-2.0
// SPDX-License-Identifier: CC-BY-SA-4.0
= Proof status — trope-checker verified core
:revdate: 2026-06-22
:toc: macro

What is *proved* (machine-checked, no axioms), *stubbed-and-named* (an open
problem stated, never asserted as done — HC-5), and *deferred*. Ground-truth:
`idris2 --typecheck verification/proofs/idris2/trope.ipkg` (exit 0). No
`believe_me`, `assert_total`, or `postulate` stands behind any claim below.

toc::[]

== Proved (machine-checked)

[cols="1,2,2",options="header"]
|===
| Lemma | Statement | Where
| Fidelity L1 | `dplus` (tropical loss addition, ⊤ absorbs ∞ absorbs finite) is associative | `Trope.Fidelity.dplusAssoc`
| Fidelity unit/refl | `Q 0` right-unit of `dplus`; `dLte` reflexive | `Trope.Fidelity.dplusZeroR`, `dLteRefl`
| Coordinate L1 | `▷` associative on fate, bond, merge | `Trope.Coords.{fateAssoc,bondAssoc,mergeAssoc}`
| Coordinate L2 | unit of `▷` on each coordinate | `Trope.Coords.{fateUnitL/R,bondUnitL/R,mergeUnitL/R}`
| Coordinate ⊑ refl | retention order reflexive on each coordinate | `Trope.Coords.{fateLteRefl,bondLteRefl,mergeLteRefl}`
| Grade L1 (assoc) | `▷` associative on the full three-coordinate grade | `Trope.Laws.gradeAssoc`
| Grade L2 (unit) | `ε ▷ g = g = g ▷ ε` | `Trope.Grade.{gradeUnitL,gradeUnitR}`
| Decidability of ⊑ | `⊑` is decidable on grades | `Trope.Laws.decGradeLte`
| Veridicality Derivation | exactly three deceptive duals, one per coordinate (Falsified/Misbound/Conflated), each the unique deceptive inhabitant | `Trope.Veridicality.{fate,bond,merge}DeceptiveUnique`, `deceptiveDuals`
| Honest Generation | the five atoms (attenuate/predicate/drop/sever/fuse) + ε generate exactly the honest fragment of each coordinate; the atoms never produce a deceptive dual | `Trope.Generation.{genFate,genBond,genMerge,movesHonest*}`
| ε honest & coherent | ε is non-deceptive and HC-4-coherent | `Trope.Veridicality.epsilonHonest`, `Trope.Grade.epsilonCoherent`
|===

== Stubbed-and-named (open problems — NOT asserted)

[cols="1,3",options="header"]
|===
| ID | Open problem
| *Grade soundness* (calculus §8) | `Γ ⊢ e : A ! g ⟹ g ⊑ ρ(in,out)` — the declared grade never claims more retention than execution delivers. Requires an operational model of execution (`ρ`); the term language is out of the verified core's scope this pass. Stated in the calculus; *not* claimed proved here. This is the headline next truth-mover.
| *L4 monotonicity / L5 infectiousness as theorems* | Hold by the bottom/absorbing structure of each coordinate (used implicitly); not yet extracted as standalone Idris lemmas. Tractable; deferred to the next pass.
| *O1 exhaustiveness of the effects* | That the five atoms (plus the three duals) are the _true generators_ of all particularity-affecting operations is a well-chosen list, not yet a theorem derived from the three axes. Calculus O1. Philosophy-of-individuation question O1-coord is logged, not coded.
|===

== Deferred (named, not attempted this pass)

* Full Haec compiler / codegen / runtime (scaffolded in the `haec` repo).
* A Rust fast-core validated against this reference via the conformance suite.
* The produce/demand adjunction (calculus O3); the fidelity-carrier question (O4).

== Modelling notes (faithful-but-simplified, recorded for honesty)

* *Provenance tags on `Fused` are erased* in the verified core (they are metadata
  that does not affect the lattice laws); the executable checker carries them.
* *Deceptive duals are modelled as each coordinate's bottom* (worst retention,
  absorbing), which is how the calculus orders `Conflated`; this makes every
  honest floor fail against a deceptive grade (HC-2) by construction.
* *L2 is stated with ε's literal value* in Idris (a bare top-level `epsilon` as a
  function argument does not reduce under the unifier); since
  `epsilon = MkGrade Present Present Present Present Intact Single` definitionally,
  the statement is `ε ▷ g = g`.
