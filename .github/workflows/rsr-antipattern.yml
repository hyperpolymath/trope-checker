# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
# RSR Anti-Pattern Check - Uses reusable workflow from standards

name: "🔴 Gate: RSR anti-pattern scan"
on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

permissions:
  contents: read

jobs:
  antipattern-check:
    uses: hyperpolymath/standards/.github/workflows/rsr-antipattern-reusable.yml@bd0df9ead7faf0cdfe0e13e7966d91e28d0101d4 # standards main pin (freezes current behaviour)
