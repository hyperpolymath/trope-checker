// SPDX-FileCopyrightText: © 2026 Jonathan D.A. Jewell (hyperpolymath) <j.d.a.jewell@open.ac.uk>
// SPDX-License-Identifier: MPL-2.0
// SPDX-License-Identifier: CC-BY-SA-4.0
= EXPLAINME — trope-checker (developer deep-dive)
:toc: macro
:sectnums:

The README is the pitch; this is how it actually works and why it is shaped this way.

toc::[]

== The one job

`check : TropeIR → Verdict`. Everything else (schema, Idris core, conformance) exists
to make that one pure function trustworthy and portable. The checker never sees a
source program — only the IR. That is the whole portability story (HC-1).

== Data flow

. *Parse + validate* the IR JSON against `schemas/trope-ir.schema.json`. Validation
  is load-bearing: HC-4 (bond⊣bearer dependency) and HC-3 (no deceptive grades)
  are enforced here, and a rejection names the coordinate.
. *Accumulate grades* over the DAG (`src/checker/tropecheck.py`): roots get ε; a
  single-input edge composes `acc(source) ▷ grade(edge)`; a `fuse` edge takes the
  retention-meet of its two inputs then composes its label.
. *Decide* `floor(use_model) ⊑ acc(output)`. If it fails, walk edges in topological
  order to find the *witness*: the first edge whose accumulated grade dropped below
  the floor in a failing coordinate.

== Why two cores

The Idris2 development (`verification/proofs/idris2/`) *defines and proves* the grade
algebra (the laws L1/L2, decidable ⊑, the veridicality and generation results). The
Python checker is the runnable sketch. They are kept in agreement by the conformance
suite — the same design the calculus prescribes (§9.3): a verified reference + a
shipping core + a corpus that forces agreement. A Rust fast-core is the deferred
third leg.

== The grade, concretely

`Grade = (fate : Φ→Fate, bond, merge)`. `Fate = Present | Attenuated δ | Predicated |
Dropped | Falsified`; `Bond = Intact | Withheld | Severed | Misbound`; `Merge =
Single | Fused τ | Conflated`. The deceptive duals (Falsified/Misbound/Conflated) are
each their coordinate's bottom: absorbing under ▷, below every honest value, so any
honest floor fails against them. Under the prevent profile they are not even
representable in a valid IR.

== Reading the proofs

`idris2 --typecheck verification/proofs/idris2/trope.ipkg`. Start in `Trope.Fidelity`
(the δ dimension), then `Trope.Coords` (the three coordinates + per-coordinate laws),
then `Trope.Grade`/`Trope.Laws` (the assembled grade + L1/L2/decidability), then
`Trope.Veridicality` and `Trope.Generation` (the structure results). What is and is
not proved is in `docs/status/PROOF-STATUS.adoc` — read it before believing any claim.

== Extending

A new front-end: see `docs/CONTRIBUTING-A-FRONTEND.adoc`. A new effect or grade
coordinate: change the calculus first, then the schema, then the Idris core, then the
checker, then the conformance suite — in that order, small verified increments (HC-5).
